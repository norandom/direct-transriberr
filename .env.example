# Docker Compose Environment Variables

# External media directory (absolute path)
MEDIA_DIR=/path/to/your/media/files

# Output directory for transcriptions (absolute path)
OUTPUT_DIR=/path/to/your/transcriptions

# Model to use (tiny, base, small, medium, large-v3)
# Leave empty for auto-detection
WHISPER_MODEL=

# Memory limit for container (optional)
MEMORY_LIMIT=8G

# Model storage directory (will be created if it doesn't exist)
# Models are downloaded once and reused on subsequent runs
MODELS_DIR=./models